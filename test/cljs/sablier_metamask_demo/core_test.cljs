(ns sablier-metamask-demo.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [sablier-metamask-demo.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
